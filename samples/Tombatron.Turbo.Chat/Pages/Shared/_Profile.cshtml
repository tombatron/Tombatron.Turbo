@model Tombatron.Turbo.Chat.Data.User

<turbo-frame id="user-profile"
             class="profile-card">
    <div class="profile-card-banner"></div>
    <div class="profile-card-avatar">@Model.Username[0].ToString()</div>
    <div class="profile-card-body">
        <h3 class="profile-card-username">@Model.Username</h3>
        <div class="profile-card-divider"></div>
        <div class="profile-card-section">
            <span class="profile-card-label">Member Since</span>
            <span class="profile-card-value">@Model.CreatedAt.ToString("MMMM yyyy")</span>
        </div>
        <div class="profile-card-section">
            <span class="profile-card-label">Roles</span>
            <div class="profile-card-roles">
                <span class="profile-role-badge">Member</span>
            </div>
        </div>

        <form method="post" asp-page-handler="StartDm" data-turbo-frame="_top">
            <input type="hidden" name="targetUserId" value="@Model.Id" />
            <button type="submit" class="profile-dm-btn">Direct Message</button>
        </form>
    </div>
</turbo-frame>
